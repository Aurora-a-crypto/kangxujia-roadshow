services:
  - type: web
    name: kangxujia-roadshow
    env: node
    plan: starter
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /s
    envVars:
      - key: HOST
        value: 0.0.0.0
